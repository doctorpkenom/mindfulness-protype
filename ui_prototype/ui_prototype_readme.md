# UI Prototype (Legacy)

The `ui_prototype` directory contains the initial minimal viable product (MVP) or older versions of the user interface.

## ⚠️ Status: Deprecated
> [!IMPORTANT]
> This folder is maintaned for reference purposes only. The active production frontend is located in the **`frontend/`** directory (React + Vite).

## 📄 Contents
- **`app.py`**: A Python-based UI (likely using Streamlit or CustomTkinter) used for early testing of the backend logic before the React frontend was built.
